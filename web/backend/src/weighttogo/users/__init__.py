"""Users bounded context.

Manages user profiles, account settings, and user-level preferences.
The users context owns the User aggregate from a profile standpoint and
provides read models consumed by other bounded contexts.
"""
