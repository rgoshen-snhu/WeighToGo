"""Infrastructure layer for the goals bounded context.

Contains SQLAlchemy models, repository implementations, and external adapters
for goal persistence. Framework and I/O imports are permitted in this layer.
"""
