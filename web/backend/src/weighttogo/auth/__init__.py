"""Authentication bounded context.

Handles user identity, credential verification, and session management.
This context owns the User aggregate from an authentication standpoint and
coordinates with the users context for profile data.
"""
